---
principles:
  - Connection acceptance advances the branch immediately.
  - The first private message should stay consistent with the stored prospect context and cadence branch.
do:
  - Confirm the connection is actually accepted in the live surface.
  - Pull the direct-message draft brief before writing the note.
avoid:
  - Do not keep waiting on the pre-accept branch once acceptance is confirmed.
  - Do not jump to fallback email first.
writeback:
  - Record the accepted state if it is not already stored.
  - Record the first private-message touch after send.
---
Use the accepted connection with {{prospectName}} at {{companyName}} to advance the branch into the first direct message. Confirm the acceptance live, pull the stored DM brief, write the message in the current chat/runtime, and then write the resulting touch and cadence update back into Exo.
