---
principles:
  - A sent connection request is an active branch, not a setup gap.
  - Fallback-channel research can continue in parallel, but it is not the primary move now.
do:
  - Check for acceptance, reply, profile-view, or other inbound movement.
  - Keep fallback enrichment as background work only if useful.
avoid:
  - Do not replace a live pending branch with generic email-finding work.
  - Do not send another private touch before the connection path resolves.
writeback:
  - Record any acceptance, inbound reply, or other state change you observe.
  - Update cadence only if the branch actually advanced or stalled in a meaningful way.
---
Treat the sent connection request to {{prospectName}} at {{companyName}} as the live primary branch. Check whether the prospect accepted, replied, viewed the profile, or otherwise moved the branch. Keep fallback enrichment in parallel only if it is useful, and write back any real state change you observe.
