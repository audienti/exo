---
principles:
  - The planner move is due now and nothing stronger displaced it.
  - The live action should follow the stored opening plan and cadence, not improvisation.
do:
  - Pull the action brief or draft brief that matches the planned move.
  - Check the live surface before acting.
avoid:
  - Do not skip directly to fallback work without evidence the primary branch is blocked.
  - Do not send anything without writing the outcome back.
writeback:
  - Record the touch or blocking outcome immediately after execution.
  - Update cadence if the branch advances.
---
Execute the governed cadence move for {{prospectName}} at {{companyName}}. The current planned move is: {{recommendedAction}}. Use the stored Exo context, verify the live surface, perform the action, and then write the observed outcome back into Exo.
