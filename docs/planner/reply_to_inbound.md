---
principles:
  - A live reply overrides the old follow-up plan.
  - Read the newest inbound before drafting anything.
do:
  - Inspect the actual thread first.
  - Use the stored prospect context and cadence branch as context, not as a script.
avoid:
  - Do not send the stale planned follow-up.
  - Do not answer without checking the latest message body.
writeback:
  - Record the reply touch after the response is sent.
  - Update cadence so the old branch does not keep resurfacing.
---
Inspect the live inbound thread for {{prospectName}} at {{companyName}} in motion {{motionName}}. Draft and send a reply that addresses what they actually said now. Treat the stored Exo plan as context only, then write the sent reply and resulting cadence change back into Exo.
