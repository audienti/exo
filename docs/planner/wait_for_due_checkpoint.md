---
principles:
  - Not every open branch needs action today.
  - Waiting is valid when the cadence window is not due and nothing stronger has happened.
do:
  - Confirm there is no newer inbound or branch change before staying idle.
  - Leave the branch ready for the due checkpoint.
avoid:
  - Do not force another touch early just to feel active.
  - Do not reopen old setup work while the branch is simply waiting.
writeback:
  - Write back only if you observe a meaningful new event while checking.
---
Hold the {{prospectName}} branch until its due checkpoint at {{dueAt}} unless a stronger inbound event has appeared. Verify that the branch is still simply waiting, then leave Exo unchanged unless the live state actually moved.
