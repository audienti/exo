---
principles:
  - An outbound touch already in flight should be given room to work.
  - Waiting is not passivity when the branch is active.
do:
  - Check for inbound movement or a due checkpoint change.
  - Keep background enrichment or prep subordinate to the live branch.
avoid:
  - Do not stack fresh outreach on top of an unresolved branch.
  - Do not reopen old setup work unless the branch is actually blocked.
writeback:
  - Write back only if a real new observation changed the branch.
---
Hold the current outbound branch with {{prospectName}} at {{companyName}} unless a stronger inbound event or a due checkpoint changes the plan. Verify that the branch is still live, and only write back if the state actually moved.
