---
affordances:
  - Comment against the stored recent-post thread when it is still fresh enough to justify engagement.
fallbacks:
  - If the post is stale, drifted, or no longer relevant, skip the public comment instead of inventing a hook.
successProofs:
  - The comment appears in the live thread under the intended identity.
failureSignatures:
  - The thread is no longer visible or no longer supports a real comment path.
  - The current page only exposes reaction or share affordances, not a usable comment path.
cleanup:
  - Re-read the posted comment once before leaving the thread so obvious misfires do not stay invisible.
---

# Comment on Post

This is a legitimacy-sensitive action.

If the context is no longer fresh enough, do not try to save it with clever copy.
